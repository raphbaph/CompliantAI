# TYPE  DATABASE     USER                                           ADDRESS      METHOD
local   all          bootstrap_admin                                             trust
local   compliantai  gateway_runtime,audit_reader,security_admin                 scram-sha-256
host    compliantai  bootstrap_admin,gateway_runtime,audit_reader,security_admin 0.0.0.0/0    scram-sha-256
host    compliantai  bootstrap_admin,gateway_runtime,audit_reader,security_admin ::/0         scram-sha-256
local   all          all                                                        reject
host    all          all                                            0.0.0.0/0    reject
host    all          all                                            ::/0         reject
